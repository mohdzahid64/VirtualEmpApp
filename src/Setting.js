import React from 'react';
import { SafeAreaView, View, Text, ImageBackground, Image, ActivityIndicator, Settings } from 'react-native';







const Setting = (props) => {

  const { navigation } = props

  
  return (

    <View style={{flex:1,alignItems:'center'}}>
        <Text>
             Settings Page
        </Text>
    </View>
  );
};

export default Setting;